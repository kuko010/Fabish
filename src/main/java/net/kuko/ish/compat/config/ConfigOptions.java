package net.kuko.ish.compat.config;

public class ConfigOptions {
    // These are NOT static so GSON can save them
    public boolean enableLibTooltips = false;
    public boolean enableFullbright = false;
}